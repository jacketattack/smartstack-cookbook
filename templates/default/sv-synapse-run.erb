#!/bin/bash

# run the ruby version of synapse
cd <%= node.synapse.install_dir %>
export GEM_HOME=<%= node.smartstack.gem_home %>
exec /opt/rbenv/shims/bundle exec ./bin/synapse --config <%= node.synapse.config_file %> 2>&1
